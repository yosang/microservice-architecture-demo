{
  "devDependencies": {
    "autocannon": "^8.0.0",
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "build": "concurrently 'cd api-gateway && npm install' 'cd catalog-bff-mobile && npm install' 'cd catalog-bff-web && npm install' 'cd inventory-service && npm install' 'cd products-service && npm install' 'cd review-service && npm install'",
    "start": "concurrently 'cd api-gateway && npm start' 'cd catalog-bff-mobile && npm start' 'cd catalog-bff-web && npm start' 'cd inventory-service && npm start' 'cd products-service && npm start' 'cd review-service && npm start'",
    "rate-test": "autocannon --connections 5 --duration 15 --maxOverallRequests 250 http://localhost:5000/api/v1/web/products"
  }
}
